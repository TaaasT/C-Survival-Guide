using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using UnityEngine.SceneManagement;


//Namespaces är det som e ovan 
// anledningen till att man lägger till dom efter behov är för att inte allt ska finnas på en gång o finnas 1000 saker av samma ord.

    
public class NamespacesInfo : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
